🎯 **What**
Refactored the `concurrent.futures` import in `src/photo_selector_toolbox/cli.py` to use explicit imports (`from concurrent.futures import ThreadPoolExecutor, as_completed`) and updated the call sites.

💡 **Why**
This resolves a false-positive unused import warning from the linter. While the module was imported, only its sub-components were used. Being explicit satisfies the linter cleanly without the need for inline suppression comments.

✅ **Verification**
Ran `poetry run black .` and `poetry run flake8` locally to confirm formatting and that the linter warning is resolved. Also verified no regressions by running the full test suite (`poetry run xvfb-run pytest`).

✨ **Result**
Clean, explicit code with one fewer linter warning, improving overall code health and readability.
