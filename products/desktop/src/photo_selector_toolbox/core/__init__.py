"""Headless domain logic: image models, EXIF-independent analysis,
sharpness/noise scoring, duplicate detection, caching, configuration
and plotting. Nothing in this package may import ``tkinter``.
"""
