# Code Health Backlog & Lessons

Refactoring candidates and structural lessons. Owned by `@shared-code-health-agent`; any agent may append. Format for backlog items:

```markdown
## [OPEN|DONE] YYYY-MM-DD - Short title
**Where:** file paths / modules
**Debt:** what is wrong and why it matters
**Proposal:** the safe refactoring, referencing `ai/skills/refactoring-guide/SKILL.md` patterns
```

Lessons use the standard `ai/memory/` format (Learning/Action).

## [OPEN] 2026-08-09 - `save_config` takes a whole file where every caller wants a field, and one caller lost settings for it
**Where:** `products/desktop/src/photo_selector_toolbox/core/config.py::save_config`, callers in `gui/app.py` (`CollectionSettingsDialog.save_settings`, `_clear_recent_folders`), `gui/sharpness_tool.py` (six load-mutate-save sites), `gui/aesthetic_settings.py::save_settings`
**Debt:** `save_config(config)` is `json.dump(config)` — it replaces `settings.json` wholesale. Every correct caller therefore has to spell out load → mutate → save, and the one caller that did not (the old Ollama settings dialog, `save_config({"ollama_url": …, "ollama_model": …, "ollama_prompt": …})`) silently erased `selection_folder`, `separate_raw_jpeg`, `recent_folders` and `aesthetic_engine` every time a user pressed Save; `load_config` then refilled them from `DEFAULT_CONFIG`, so the loss reads as "my Selection folder reset itself" rather than as an error. Fixed at that call site while rewriting the dialog (a regression test now asserts an unrelated key survives a save), but the shape that invites the mistake is still the only API. Out of scope here: `core/` belongs to another agent, and three agents were editing this product concurrently.
**Proposal:** Add `update_config(**fields)` (or `update_config(mapping)`) to `core/config.py` that does the load-mutate-save once, and migrate the eleven call sites to it; keep `save_config` for the whole-file case (`_clear_recent_folders`, migrations). Behaviour-preserving, per `ai/skills/refactoring-guide/SKILL.md`; cover it with a test that a partial update leaves untouched keys intact.

## [OPEN] 2026-08-09 - `generate_icons.py` writes to a pre-reorganisation Android path and never produced the macOS `.icns` its requirement claims
**Where:** `products/desktop/scripts/generate_icons.py`, `products/desktop/scripts/build.py::generate_icons_if_possible`, `docs/products/desktop/REQUIREMENTS.md` § 4.2 "macOS Icon"
**Debt:** The script resolves `assets/logo.png` relative to the cwd (the build runs with `cwd=products/desktop`, where no `assets/` exists) and writes launcher icons into `android/app/src/main/res` — a path that stopped existing when the Android products moved to `products/android/<product>/`. On Linux CI it therefore prints "Error: assets/logo.png does not exist", returns `None`, and the build carries on with pre-generated assets, so nothing fails. Separately, § 4.2 states "the build script must generate and bundle a macOS-compatible `logo.icns` file from primary assets" — no code in `scripts/` generates `.icns`; `assets/logo.icns` is a committed artefact. Two silent falsehoods: a generator that generates nothing, and a requirement describing a step that does not exist. Not fixed here: the icon generator writes into the Android products' resource trees, which belongs to `@android-shared-build-agent`, and the `.icns` question is a requirement decision, not a bug fix.
**Proposal:** Split it. Either delete the Android branch of `generate_icons.py` (the icons are committed and regenerating them from a PNG is not how Android adaptive icons should be produced) or move it under `@android-shared-build-agent` with repo-root-anchored paths. Then either implement `.icns` generation from `assets/logo.png` (`iconutil` on macOS, `png2icns` elsewhere) or rewrite § 4.2 to say the `.icns` is a committed asset that the build bundles. Do not leave the requirement asserting a capability nothing implements.

## [OPEN] 2026-08-09 - A portrait preview carries 44 % fewer pixels than a landscape one at `CACHE_SIZE`
**Where:** `products/desktop/src/photo_selector_toolbox/gui/image_panels.py` (`CACHE_SIZE`), `products/desktop/src/photo_selector_toolbox/core/utils.py::load_image_preview`, `docs/products/desktop/REQUIREMENTS.md` § 3.1
**Debt:** Requirement 3.1 pins the preloader cache at a landscape-shaped `1200x900` box, and `thumbnail()` fits into it. Now that EXIF orientation is applied (2026-08-09), a portrait photograph fits as **600x900 = 0.54 MP** where a landscape one fits as **1200x800 = 0.96 MP** — measured on real Sony files (stored 6192x4128, `Orientation=8`). Before the orientation fix the same file came back 1200x800 because it was treated as landscape, so the fix did not create the asymmetry, it revealed it. The consequence is that portrait shots are the ones that go soft first when the panel is scaled up, which is the opposite of what a culling tool wants — portraits are the images a photographer leans in on. Not fixed in the orientation pass because the box is requirement-pinned and the right shape is a UX question (a square `1200x1200` box costs ~33 % more RAM per cached landscape image; an area-based budget keeps the pixel count constant but makes the cache size non-obvious).
**Proposal:** Decide with `@shared-ux-agent` between a square cache box and an area budget (e.g. "fit to ~1 MP, whatever the aspect"), then change `CACHE_SIZE` and requirement 3.1 together. Measure the preloader's memory ceiling at the chosen shape before committing — `image_panels` holds several of these at once.

## [OPEN] 2026-08-09 - PhotoTok classifies portrait vs landscape from stored pixels and ignores the EXIF orientation
**Where:** `products/android/phototok/src/com/phototok/data/source/LocalImageSource.kt::readImageDimensions`, `products/android/phototok/src/com/phototok/data/model/ImageItem.kt::isLandscape`, `products/android/phototok/src/com/phototok/domain/PhoneFeedOrdering.kt`
**Debt:** Found while fixing the same defect class on Desktop (`load_image_preview` ignored the orientation tag). `readImageDimensions` uses `BitmapFactory.Options(inJustDecodeBounds = true)`, which reports the **stored** dimensions; `ExifInterface.ORIENTATION_*` is never consulted. `ImageItem.isLandscape` is then `imageWidth > imageHeight`, and the "sort by orientation" feed setting splits the feed on it — so every rotated camera JPEG (Sony bodies write `Orientation=8`, which swaps the axes) lands in the wrong section. Coil renders those photographs correctly, so the pictures look right while the grouping is wrong, which is the hard version to notice. Not fixed here: different product, and it belongs to `@phototok-core-agent`.
**Proposal:** Read `ExifInterface(...).getAttributeInt(TAG_ORIENTATION, …)` alongside the bounds decode and swap width/height for orientations 5-8 before storing them, so `isLandscape` means what its KDoc says. Cover it with a unit test over the eight orientation values, as `products/desktop/tests/unit/core/test_image_orientation.py` now does for Desktop.

## 2026-08-09 - Four Defects That Returned Plausible Values, and the Three Oracles That Would Have Caught Them
**Learning:** One Desktop task turned up four long-lived defects sharing a single property: nothing anywhere raised, so every gate the project owns stayed green while the photographer looked at the damage. `core/utils.py::load_image_preview` never applied EXIF `Orientation` — and a sideways photograph is a valid image. `tools/aesthetic.py::map_apple_score_to_10` mapped Apple Vision's `overallScore` from an **assumed** effective range of `[-0.5, 0.5]` rather than the documented `[-1, 1]` — and 10.0 is a valid score: measured over 84 real photographs the raw scores ran -0.042 to 0.915 (median 0.565), so **59 of 84 (70 %) clamped to exactly 10.0 and the entire library held 15 distinct values**, a metric that exists to rank and could not rank. The engine dispatcher's `auto` branch silently fell through to a backend ~197× slower — and a slow score is a valid score. A settings dialog wrote three keys through a whole-file `json.dump`, erasing four others — and a `settings.json` that `load_config` refills from `DEFAULT_CONFIG` is a valid settings file. All four areas had unit tests; none could have caught any of it, because each asserts one output of one path and every one of those outputs was individually plausible. The orientation defect adds the sharpest tell: it lived in a three-branch decoder whose RAW `postprocess` branch was already correct (libraw applies the camera flip itself), so previews and fullscreen had **disagreed with each other on the same file** for months and nothing compared them.
**Action:** Where a function cannot fail loudly, an assertion on a single output is not a test of it — choose an oracle that is a property of a **population** or of an **agreement**. Three, each cheap enough to be routine: (1) *agreement* — when one value has several producers (decode branches, platform paths, a cached and an uncached route), assert they return the same answer for the same input; a branch that is already correct because the library does the work for you is the reason to write that test, not to skip it. (2) *distribution* — anything mapping an external value onto a display or ranking scale gets run over a real corpus and asserted on shape: distinct-value count, and the fraction pinned at each clamp. A ranking metric fails by collapsing, never by erroring. Map onto the range the vendor **documents**; narrowing it to "what real photographs actually do" is a measurement claim, and if you have not run the corpus you have not made it. (3) *observability of a choice* — an `auto`/fallback selection returns `(choice, reason)` and logs it, and since a mocked probe only ever confirms what you mocked, assert additionally what the **default install** selects. Corollary for writers: when a whole-payload writer is handed a partial payload, the loss is invisible by construction — cover every partial save with an assertion that an unrelated key survived it.

## 2026-08-08 - A Coordinator's Unmeasured Number Becomes Everyone Else's Established Fact
**Learning:** Three times in one session a coordinating agent emitted a figure that *sounded* measured and was not, and each travelled further than the same error from a subagent would have. A "~146 dp height leak" was reasoned backwards from arithmetic against the window height, then written into `code_health.md` as a finding and into two agent briefs as an established diagnosis — in the same briefs that instructed the agent to measure rather than guess. There was no leak: the coordinator's model shared the failing test's assumption about which constraint binds, so it reproduced the same artefact, and the agreement between model and test was read as corroboration when it was one observation counted twice. Separately in the same session an emulator was left in the wrong rotation and the resulting "7 % frame" was reported as a finding, and a baseline-counting regex under-reported the pre-existing failures. The delegating agent's output does not arrive as advice; it arrives as a premise. It gets copied into briefs, backlog items and `ai/memory/`, where it outlives the session, and disproving it costs the subagent a full verification pass — here the agent's first act was to measure, which took minutes and voided the brief it had been given.
**Action:** Every quantity that leaves a coordinator carries its provenance: **measured** (name the command and the device it came from), **computed** (state the inputs and the model, and label it a prediction to be checked), or **assumed**. Only measured numbers may be written into `ai/memory/`, into a `[OPEN]` backlog item, or into a brief's findings; a computed number goes in as the hypothesis it is — and where the measurement is cheap and the subagent has the device, brief the *question* ("measure the frames at reference geometry and tell me which constraint binds") instead of the answer, because a wrong answer costs more than no answer. Treat your own model agreeing with a failing test as one data point, not two: name the assumption they share before calling it confirmed. When a number does turn out wrong, chase every place it was copied to — the brief, the backlog entry, the requirements line — the retraction does not propagate on its own.

## 2026-08-08 - A Merge That Replaces the Whole Item Is a Rollback, and a Dispatcher Hop Is Where It Hides
**Learning:** `restoreCachedScores` snapshotted the image list, spent one Room read per photograph on `Dispatchers.IO`, then folded the **pre-IO snapshot** back over live state: `if (image.scanResult != null) image else byUri[image.uri] ?: image`. It reads as a merge and is a rollback — every image without a score is replaced by its stale copy, which in an unscanned folder is all of them, so the EXIF another coroutine wrote during the IO window was discarded. EXIF is only re-fetched on navigation, so the frame the photographer was looking at simply stayed blank. `runScan` had the same snapshot-mutate-write-back shape. Two things kept it invisible: the only route to the code was a `withContext(Dispatchers.IO)` hop, which escapes `runTest`'s virtual time, so every attempt to assert it through the ViewModel was a race rather than a test — the tell is that adding `advanceUntilIdle()` changes nothing, because the work is not on the test scheduler at all; and the fakes pre-populated `exifData`, so the production *arrival order* — EXIF landing after the snapshot was taken — was never exercised.
**Action:** Merge the field you computed, never the item you computed it from. Express it as a pure function over `(items, computedValues)` that has no way to say "take the whole old item" — `domain/session/ScoreMerge.apply` is the worked example, and `ScoreMerge.scoresOf` exists so a caller cannot hand items where a field map is wanted. Anything captured before a suspend point is input, never output: on the way back, re-read current state and write only the field. Logic reachable only through a dispatcher hop does not get tested, so extract it pure *before* writing the test rather than after failing to — if a test needs `advanceUntilIdle` to settle a data question, and still fails, the data question is in the wrong layer. And build fakes that reproduce the production arrival order, not just its end state: a fixture that supplies up front what production computes later erases the exact window every merge bug lives in.

## 2026-08-08 - A Test Fixture That Routes Around Production Code Becomes a Production Feature
**Learning:** Both `:android-desktop` suites — about 50 call sites — opened `gdrive://` folders on purpose, documented at `SelectorViewModelTest.kt:51` as the way to skip SAF's `DocumentFile` permission dance. That quietly made the Google Drive source load-bearing for the tests, even though it was redundant as a feature (SAF already mounts Drive as a document provider): deleting its branch dropped every one of those call sites into `DocumentFile.fromTreeUri` at once, and the resulting failures present as layout and timing bugs, nowhere near the code that was removed. The tests reached for a second URI scheme because the interface offered no seam — the ViewModel called `takePersistableUriPermission` and `DocumentFile` itself, and a `DocumentFile` cannot be faked at all.
**Action:** When a test takes a documented shortcut past production code, read it as a missing seam in the production interface rather than as test convenience; the shortcut will be inherited by the next fifty tests and will hold the bypassed code in place. Put the capability the test actually needs on the interface — `ImageRepository.openFolder(context, uri): String?` persists the grant and confirms the folder resolves, so nothing above the repository touches SAF — and let the fake satisfy the interface instead of an alternate code path. No caller above a storage interface may branch on a URI scheme. Before deleting any feature, grep two places the diff will not show you: the **test sources**, where the migration cost actually lives (seam first, deletion second — that is two changes and should be two), and the **persisted keys**, for values only that feature could have written. A retired `gdrive://` URI left in DataStore greets a returning photographer with "permission revoked or directory deleted" for a folder they never lost, so the read must drop it (`SettingsRepository.lastFolderUri`) rather than the writer merely stopping.

## 2026-08-08 - A Cross-Cutting Duty That Lives Only in a Role Description Does Not Happen
**Learning:** Two mechanisms existed to carry an improvement from one product to its siblings, and neither closed the loop. `ai/memory/bolt.md` (2026-07-31) recorded the SAF-enumeration anti-pattern as a general rule from PhotoTok, and Android Desktop still contained that exact anti-pattern — plus a per-file `openInputStream` the lesson did not cover — more than a week later; `ai/memory/palette.md` the same day prescribed coach marks over label lists, and Android Desktop kept the label list. `docs/shared/FEATURE_PARITY.md` tracks only Desktop→Android *features* and has no row for interaction or performance patterns, and the obligation to evaluate parity sat in `ai/ROUTING.md` as a coordinator duty — a sentence in a role description rather than a step in the checklist that actually runs at the end of every task. Writing a lesson down is a diagnosis, not a distribution mechanism: agents read `ai/memory/` for the product they are already working in, so a lesson learned in one product has no route into another. The user, not the framework, eventually noticed.
**Action:** Put every cross-cutting obligation in the checklist that executes, never only in the role that owns it — an unchecked duty is decoration. When a fix or lesson lands, name the sibling products it could apply to and record the decision, *including* "not applicable, because…", in the shared ledger where the next agent will look, not in the task summary that disappears with the session. And when the same lesson appears in `ai/memory/` twice from two products within days, read it as proof the propagation step is missing rather than as two lessons.

## [DONE] 2026-08-08 - `SelectionActionLabels.confirmation` hard-codes "Selection" while the folder name is a setting
**Landed 2026-08-08:** every function in `SelectionActionLabels` (and `FilingAction.phraseFor`, `SelectorShortcut.describeEffect`, `SelectorHintText.title`) now takes `selectionFolderName`, defaulting to the new `FilingAction.DEFAULT_SELECTION_FOLDER`; the hint heading is generated by `SelectionActionLabels.confirmation` so the card and the snackbar are one sentence; the button keeps the bare verb and only the phrase/snackbar name the folder. `SelectionActionLabelsTest` asserts a renamed folder reaches the snackbar, that the euphemism ban still holds, and that the wording default equals `SettingsRepository.DEFAULT_SELECTION_FOLDER_NAME`. `SelectorViewModel`'s two inline literals are the UI agent's to swap over.

**Where:** `products/android/android-desktop/src/com/photoselectortoolbox/domain/format/SelectionActionLabels.kt`, and `FilingAction.phrase` beside it
**Debt:** `confirmation(action)` returns `"Copied to Selection"` / `"Moved to Selection"`, and `FilingAction.phrase` is `"Copy to Selection"`, but the destination folder name is a user setting (`selection_folder_name`, DataStore, default `Selection`). A photographer who renames it to `Picks` gets a snackbar and a button phrase naming a folder that does not exist. This is a milder form of the euphemism defect the same file's test guards: the words do not describe what actually happened. Noticed while writing `SelectorHintText`, which *does* take the folder name and therefore now disagrees with the snackbar that fires alongside it.
**Proposal:** Thread the configured folder name into `SelectionActionLabels` (a parameter with the default as fallback, mirroring `SelectorHintText.message`) and into the `ViewModel`'s snackbar wording; extend `SelectionActionLabelsTest` to assert a renamed folder appears. Purely additive to a pure object, so it is verifiable without a device. Keep `phrase` fixed-width enough for the 190dp control — if the name is long, the button keeps the verb and only the accessibility phrase and the snackbar name the folder.

## [DONE] 2026-08-08 - Drive move passes the destination folder as the file's *old* parent
**Superseded 2026-08-08:** the Google Drive source was removed entirely — SAF mounts Drive as a document provider — so `driveMoveCopy`, `GoogleDriveClient.parentsOf` and `ImageRepositoryDriveMoveTest` no longer exist. Kept as history; do not grep for these symbols.

**Landed 2026-08-08:** `GoogleDriveClient.parentsOf(fileId)` reads the real parents via `files.get?fields=parents` (`null` = unknown, empty = genuinely none), `moveFile` now takes a *collection* of parents to remove so the old single-id signature cannot invite the bug back, and `driveMoveCopy` removes every real parent, no-ops when the file is already in the destination, and **fails** rather than guessing when the parents cannot be read. Seven fake-client tests in `ImageRepositoryDriveMoveTest`; three of them were confirmed to fail against the old behaviour by mutation. No OAuth or data-flow change (same scope, same endpoint family), so no publish-agent consult.

**Where:** `products/android/android-desktop/src/com/photoselectortoolbox/data/repository/ImageRepositoryImpl.kt` — `driveMoveCopy`
**Debt:** `driveClient.moveFile(fileId, oldParentId = destFolderId, newParentId = targetFolderId)` passes the *destination* folder id as the parent to remove. Drive's `addParents`/`removeParents` then removes a parent the file does not have, so the file gains the Selection folder as an additional parent while remaining in its original folder — a Drive "move" that is really a link, leaving the photograph visible in both places after the list has already removed it. Noticed while changing the signature to return `FileOperationResult`; not fixed in that pass because the correct old parent needs a `files.get?fields=parents` lookup that does not exist on `GoogleDriveClient` yet, and a Drive-behaviour change wants its own verification.
**Proposal:** Add a `parentsOf(fileId)` call to `GoogleDriveClient` (same scope, same endpoint family as the existing metadata reads — no OAuth or data-flow change), pass the real current parent to `moveFile`, and cover it with an instrumented or fake-client test. Coordinate with `@shared-publish-agent` only if the field set requested changes what leaves the device.

## [OPEN] 2026-08-08 - Desktop and PhotoTok name "Selection" in wording while the folder is configurable
**Where:** `products/desktop/src/photo_selector_toolbox/gui/sharpness_tool.py` (~502, ~1028), `gui/fullscreen_viewer.py` (~63), `gui/app.py` (~1274) — and `products/android/phototok/src/com/phototok/` filing wording
**Debt:** The sibling of the Android Desktop defect fixed on 2026-08-08, found while recording parity. Desktop's filing buttons and shortcut list read "Move to Selection" / "Copy to Selection" while `config["selection_folder"]` is user-editable in the same app (its *log* lines already print the real `log_path`, so the product disagrees with itself). PhotoTok has the same `selection_folder_name` setting. The words do not describe what happens to the file — the same class of defect as the banned euphemisms, one step milder.
**Proposal:** Per product, in its own stack: derive the phrase from the configured folder name at render time, keep the button compact where space is tight (Android Desktop keeps the bare verb on its 190dp control and names the folder only in the accessibility phrase, the menu row and the snackbar), and cover it with a test that a renamed folder reaches the wording. Not a shared module — `products/android/core/` holds EXIF only, and Desktop is Python. Recorded in `docs/shared/FEATURE_PARITY.md` § 6.

## [DONE] 2026-08-08 - The instrumented suite is red at HEAD, so nobody can tell a regression from the status quo
**Landed 2026-08-08:** both halves. `NavigationTest` now clicks `nav_<route>` test tags instead of reaching for the retired `NavigationRail`'s semantics, and the frame-height assertion is expressed as a *function of the measured window* — it re-derives the expected frame through `FrameGeometry.imageRegion` + `threeUpLayout` and applies the absolute 440dp floor only where the window is at least `FrameGeometry.ReferenceWindowHeight` (900dp), so a 1280×800 CI emulator reports "smaller than the product targets" by staying silent rather than by failing. The fixed 45 % threshold is gone: it was reporting the display's own ceiling as a defect, because at 1480×924 a 16:9 frame is width-bound at 684×385 and 385/924 is 41 % on any ~1.6:1 window. Verified green: 36/36 on a `medium_tablet` AVD resized to the reference 2960×1848 @ 320dpi.
**Where:** `products/android/android-desktop/tests/instrumented/com/photoselectortoolbox/ui/NavigationTest.kt`, `SelectorScreenTest.kt` (`threeUpLayout_framesUseTheHeightTheDisplayAllows`)
**Debt:** `:android-desktop:connectedDebugAndroidTest` fails **5 of 27 tests on unmodified `HEAD` (180ec23)**, verified by running it in a clean worktree on a `medium_tablet` AVD (1280×800dp, natural landscape). Four are `NavigationTest` (`navigateBackToSelector`, `navigateToDuplicates`, `navigateToStatistics`, `navigateToSettings`), all `Failed to inject touch input … could not find any node that satisfies ((OnClick is defined) && hasAnyDescendantThat(ContentDescription = 'Duplicates'/…))` — the sidebar rewrite replaced the `NavigationRail` these tests reach for, and they were never updated. The fifth is `threeUpLayout_framesUseTheHeightTheDisplayAllows`: the frame is **328.5dp of an 800dp window (41 %)** against a `≥45 %` assertion. That threshold comes from `REQUIREMENTS.md` § 2 and was calibrated on the 1480×924dp reference device; the 88dp sidebar and 76dp filmstrip are fixed sizes, so they take proportionally more of a smaller window and the ratio falls below the floor without anything being wrong. This is the more expensive half of the debt: a suite that is always red trains everyone to ignore it, and it is exactly the gate `ai/memory/code_health.md` (2026-07-27) says an agent cannot evaluate before pushing. The 2026-08-08 UX overhaul added 7 instrumented tests that all pass and changed none of these 5 — but establishing that took a full baseline run in a second worktree, which is the tax this debt charges on every future branch.
**Proposal:** Two separate fixes. (1) Update `NavigationTest` to the sidebar's actual semantics — the sidebar items carry test tags already, so match `hasTestTag` rather than a `ContentDescription` on an ancestor with `OnClick`. (2) Decide whether the 45 % floor is a property of the *design* or of the *reference device*: either express it as a function of window height (with a lower floor for windows under ~900dp) or assert it only on a window at least as large as the reference. Do not simply lower the constant — the floor exists because "the images look small" is not something a suite notices on its own (`ai/memory/palette.md` 2026-08-07). Until both land, treat a red instrumented run as uninformative and diff it against a baseline worktree.

## [DONE] 2026-08-08 - The selector's control flank collapses to zero width when the readouts are hidden
**Landed 2026-08-08:** `threeUpLayout` no longer has a zero branch — `requestedFlank` falls back to `MinimumControlBlockWidth` (+ strip space) when the readouts are off, and the quarter-of-region cap does the narrow-window work the zero was doing. Row 1 now also centres explicitly in both `ThreeUpSelectorLayout` and `SelectorCoachOverlay`. The test that asserted the zero is replaced by three: the floor holds for every combination of the view toggles, the row never overflows the region at 700/1000/1376/2000 dp, and (instrumented, at reference geometry) the frame stays centred with the readouts hidden. This shipped as a **user-reported** bug — "the picture is on the left", "there are no controls around it" — which is the cost of leaving a pinned defect open; see the palette.md entry of the same date.

**Where:** `products/android/android-desktop/src/com/photoselectortoolbox/ui/selector/FrameGeometry.kt` (`threeUpLayout`, the `needsFlank` branch), `ThreeUpSelectorLayout.kt` (the control flank's `Modifier.width(flankWidth)`)
**Debt:** With `detailsVisible = false` and the filmstrip off, `threeUpLayout` returns `flankWidth = 0.dp` — and the layout still draws `SelectorControlBlock` inside a `Modifier.width(flankWidth)` box, so the entire control block is measured at 0 dp. The readouts are optional; Move, Copy, Delete, Fullscreen, Prev, Next and the four view toggles are not, and one of them is the toggle that turns the readouts back on. A unit test (`hiding the readouts gives the whole width to the frames`) actively asserts the zero, so the defect is pinned rather than merely present. Found while moving the filmstrip into that flank; not fixed in that change because the zero-flank branch is also the escape hatch that keeps a 700 dp Medium window from being squeezed to nothing, and swapping it for an unconditional 260 dp minimum would need its own measurement pass on a portrait tablet.
**Proposal:** Give the control block its own floor independent of the readouts — the flank is `max(MinimumControlBlockWidth, …)` always, with the existing quarter-of-the-region cap doing the narrow-window work the zero branch is currently doing — and replace the unit test with one asserting the block never measures below its minimum. Verify on a 700×1100 dp window before and after; the frames are 290 dp wide there today and must not get smaller.

## [OPEN] 2026-08-08 - Tablet portrait is specified to move chrome below the frames and does not
**Where:** `docs/products/android-desktop/DESIGN.md` §15.1, `products/android/android-desktop/src/com/photoselectortoolbox/ui/selector/ThreeUpSelectorLayout.kt`
**Debt:** §15.1 says that in tablet portrait — still Medium, still this layout — the readouts and controls move *below* the frames and the filmstrip returns to a horizontal row, because in portrait height is the surplus axis and the flanks are what has run out. None of that is implemented: the layout is the same landscape arrangement at every size, and the only portrait-aware behaviour is that the frame solver caps each flank at a quarter of the region and drops the filmstrip when the flank cannot hold it beside the view toggles. That is the safe half — nothing overlaps and nothing collapses — but a 700 dp window spends both flanks on chrome that would be free below the frames, and the document describes a screen that does not exist.
**Proposal:** Either implement the portrait arrangement behind the same solver (a `stacked` variant of `threeUpLayout` that returns flank height instead of flank width, chosen by comparing the region's aspect against the frame's), or cut §15.1's claim down to what ships. Do not leave the doc describing the unimplemented half — a specification nobody has built reads exactly like a regression to the next agent measuring the screen.

## [OPEN] 2026-08-07 - Compose UI in `:android-desktop` has no JVM-testable layout layer beyond `FrameGeometry`
**Where:** `products/android/android-desktop/src/com/photoselectortoolbox/ui/selector/`
**Debt:** The selector rewrite extracted its sizing arithmetic into `FrameGeometry`, which is a pure object with a real unit-test suite — and that is the only part of the layout that can be verified without a device. Everything else (overlay placement branches, control-block arrangement, the maximised state) is verified only by instrumented tests, which need an emulator and so are the gate most likely to be skipped. The same asymmetry already exists in the desktop product's Tkinter GUI tests.
**Proposal:** Continue pulling layout *decisions* out of composables into pure functions the way `FrameGeometry.overlayFitsOutside` was — placement predicates, visibility rules, ordering — so the instrumented suite is left asserting only what genuinely needs a rendered tree (bounds equality, overlap). Do not add more `if` branches inside composables that decide geometry.

## [OPEN] 2026-08-07 - `SelectorScreen` is approaching the size where orchestration and policy blur
**Where:** `products/android/android-desktop/src/com/photoselectortoolbox/ui/selector/SelectorScreen.kt` (~560 lines)
**Debt:** It owns folder pickers, four dialogs/sheets, drag-and-drop, the keyboard map, snackbar state and the context menu. `handleSelectorKey` was already extracted as a pure function for testability; the sheet-visibility state (six `remember { mutableStateOf }` flags plus the derived `sheetOpen`) is the next thing that wants to be one modelled type rather than six booleans that must be kept in sync by hand in two places. **Worse since 2026-08-08:** the coach-mark guide added a seventh visibility source that is *half* in the ViewModel — `showGuide` is local, `uiState.showIntroTour` is derived state, and `guideOpen`, `openGuide` and `dismissGuide` are three more locals reconciling them, with the maximised-state guard hanging off `guideOpen`. The sealed type below should absorb it.
**Proposal:** Introduce a `SelectorOverlay` sealed type (none / scanConfig / shortcuts / contextMenu — `legend` and `drivePicker` are both gone: the legend is now the coach overlay, and the Drive source was removed) so "which overlay is open" and "are shortcuts suppressed" derive from one value. Behaviour-preserving, covered by the existing instrumented suite. Per `ai/skills/refactoring-guide/SKILL.md`; do not combine with behaviour changes.

## [OPEN] 2026-08-05 - Split the two oversized Tkinter modules
**Where:** `products/desktop/src/photo_selector_toolbox/gui/sharpness_tool.py` (~3 300 lines), `products/desktop/src/photo_selector_toolbox/gui/app.py` (~1 700 lines)
**Debt:** Both are far past the size where a reviewer can hold them in their head, and both mix window construction, event wiring, scan orchestration and per-image action handling in one class. This is also why `products/desktop/tests/unit/gui/` needs so much module-level mocking to run at all.
**Proposal:** Carve `sharpness_tool.py` into feature modules along the seams that already exist (scan configuration, comparison layout, action handling, keyboard bindings), keeping the public class as a thin composition. Behaviour-preserving, test-covered before and after, per `ai/skills/refactoring-guide/SKILL.md`. Do **not** mix this with behaviour changes. The 2026-08-05 restructure moved these files into `gui/` without splitting them, deliberately: a move is verifiable, carving up a 3 300-line class is not, and doing both at once would make the change unreviewable.

## [OPEN] 2026-08-05 - `int.is_integer()` breaks the declared Python floor
**Where:** `products/desktop/src/photo_selector_toolbox/core/utils.py` (focal-length label formatting)
**Debt:** The code calls `.is_integer()` on a value that can be an `int`. `int.is_integer()` only exists from Python 3.12, but `products/desktop/pyproject.toml` declares `requires-python = ">=3.10,<3.15"`. On 3.10/3.11 `products/desktop/tests/unit/core/test_visualizer.py::test_get_focal_length_plot` fails with `AttributeError: 'int' object has no attribute 'is_integer'`. CI evidently runs a newer interpreter, so this is invisible there and hits contributors on the declared minimum instead.
**Proposal:** Coerce to `float` before the check, or compare `fl == int(fl)`. Add the 3.10 interpreter to the desktop CI matrix so the declared floor is actually tested — otherwise the version constraint is a claim nobody verifies.

## [OPEN] 2026-08-05 - GUI test files are not independently runnable
**Where:** `products/desktop/tests/unit/gui/`
**Debt:** Pre-existing and previously recorded below; re-confirmed during the 2026-08-05 restructure. `products/desktop/tests/unit/core/test_utils.py::TestLoadImagePreview` passes alone and fails when run after `tests/gui`-adjacent modules, because module-scoped fixtures install `MagicMock`s into `sys.modules` and never remove them. This makes any test-layout change look like a regression when it is not, and it cost real time to rule out during the restructure.
**Proposal:** As proposed below — fix the fixture to patch attributes on the real module rather than replacing `sys.modules` entries, and add a shared tk stub installed from `conftest.py`. Until then, do not treat a subset failure as evidence without checking the same subset on the previous commit.

---

## 2026-09-26 - Unmocked Tkinter Native Dialogs Block the Main Thread and Hang Headless CI Until Timeout
**Learning:** In Tkinter, modal dialogs (`tkinter.messagebox` methods like `showinfo`/`showerror` and `tkinter.filedialog` methods like `askdirectory`/`askopenfilename`) invoke native OS synchronous modal loops (`[NSAlert runModal]` on macOS, `MessageBoxW`/`GetOpenFileNameW` on Windows). When automated tests trigger an unmocked code path that opens a dialog in headless CI or without user interaction, execution freezes indefinitely waiting for human input. Because the runner does not crash or raise an exception, the test suite produces no traceback and hangs until the CI job timeout (up to 6 hours on GitHub Actions).
**Action:** In Tkinter test suites, always provide an autouse fixture in the root `conftest.py` that monkeypatches `tkinter.messagebox` (`showinfo`, `showerror`, `showwarning`, `askyesno`, `askokcancel`) and `tkinter.filedialog` methods to safe no-ops or default responses (e.g. `None`, `False`, or empty paths). Tests that explicitly assert dialog contents or user choices must mock these functions locally; the global fixture ensures that unexpected dialog triggers in unrelated unit or integration tests fail cleanly or continue rather than freezing CI runners.

## 2026-07-27 - The Local Test Runner Enforced a Strict Subset of CI, So "Green Locally" Meant Nothing (extended 2026-08-08)
**Learning:** `scripts/run_tests.sh` — the script every agent doc pointed at as *the* test gate — ran `pytest` and `gradlew testDebugUnitTest` and nothing else. CI ran four gates it did not: flake8 (a **separate job that gates the entire desktop pipeline**, so a lint error fails everything before one test executes), `--cov-fail-under=60`, the `-m "not visual"` split, and `assembleDebugAndroidTest`. An agent could therefore run the sanctioned command, see green, push, and fail CI on a gate it had no way to evaluate. `ai/skills/self_improvement/SKILL.md` Phase 2 asked "did tests pass?" without naming a command, so nothing forced the gap closed. The visible symptom was chains of blind `fix(ci)`/`fix(test)` commits: on `feat/score-chips-and-first-run-hints`, nine consecutive commits, each a guess adjudicated by a 15-minute CI run.
**Action:** A gate that exists only in CI is a gate that cannot be satisfied before pushing. `scripts/run_tests.sh` is now an explicit CI mirror: it runs every workflow gate in the same order with the same flags, and prints a per-gate PASS/FAIL/**SKIPPED** summary mapping each to its CI job. Skipped gates must be named in the task summary rather than silently treated as passes. When you change a gate in `.github/workflows/`, mirror it in `scripts/run_tests.sh` and `docs/build/CI_PARITY.md` **in the same commit**.
**Extended 2026-08-08:** naming a gate SKIPPED is necessary and not sufficient. `connectedDebugAndroidTest` needs a device, so it is skipped on every developer machine and its CI output is read by nobody — it stood **red at `HEAD` for over a week**: four `NavigationTest` cases reaching for a `NavigationRail` the sidebar rewrite had retired, and one asserting a fixed percentage floor that a CI emulator smaller than the reference device can never meet. Every branch in that window paid the same tax, running a baseline in a second worktree to tell its own regressions from the status quo. A gate nobody can run locally decays to red by default, and a permanently red suite trains everyone to stop reading it — the same failure the CI mirror was built to end, one level further out. The first time you run a device-gated suite on a branch, establish the baseline on unmodified `HEAD` **before** believing any failure; file red-at-`HEAD` as its own backlog item rather than absorbing it into your change, and fix it before adding tests to that suite. And write its assertions as functions of the *measured* device, with absolute floors guarded by a device-class predicate — otherwise the gate reports CI hardware as a product defect (`ai/memory/palette.md` 2026-08-08).

## 2026-07-27 - Compose Semantics-Tree Mistakes, Not Compile Errors, Are the Bulk of Emulator-Only CI Thrash
**Learning:** Classifying the nine `fix(test)`/`fix(android)` commits on `feat/score-chips-and-first-run-hints`: exactly **one** (2431d41) was a compile error (`hasTag`→`hasTestTag`, `item.path`→`item.uri`) — the class `assembleDebugAndroidTest` now catches locally. The other **eight** were the same narrow family of semantics-tree errors: `onNode` throwing on multiple matches, missing `useUnmergedTree = true` inside `ModalBottomSheet`, and unscoped `hasText` matching both a chip and a legend row. Adding the compile gate alone would have prevented one of nine commits. The remaining eight were preventable only by convention, because they are runtime behaviours of the Compose semantics tree.
**Action:** Codify the conventions rather than discovering them per-branch (now in `ai/agents/android-desktop-ui-agent.md` rule 9 and `docs/build/CI_PARITY.md`): prefer `onAllNodes(...).onFirst()` over `onNode(...)` for anything that can match more than once; pass `useUnmergedTree = true` when asserting inside a semantics-merging container; scope with `hasAnyAncestor(hasTestTag(...))` instead of bare text; and import Compose test matchers explicitly, never `androidx.compose.ui.test.*`. Instrumented tests legitimately need a device — they assert on a live semantics tree with Hilt injection and a real Activity lifecycle — so they stay in `androidTest`; what changed is that their *compile* failures no longer need one.

## 2026-07-27 - One Coverage Threshold Across Three OS Runners Is a Per-OS Lottery
**Learning:** `desktop.yml` applied `--cov-fail-under=60` on ubuntu, macOS **and** Windows. But `products/desktop/tests/conftest.py` skips by platform (`linux_only`/`mac_only`/`windows_only`) and skips `gui_required` without a display, so each runner executes a different subset and reaches a different coverage number. A change could pass on a contributor's Mac and fail the threshold only on the Windows runner — a CI-only failure with no local reproduction, which is precisely the condition that produces speculative fix commits.
**Action:** Enforce a coverage threshold on exactly one runner (Linux, which executes the largest subset). Other platforms still run the full suite and still fail on real test failures. More generally: a numeric gate must not be applied across environments that legitimately produce different numbers. Any test that only works on one platform must carry its marker — `conftest.py` can only skip what is marked.

---

## [DONE] 2026-07-27 - No Repository-Level Undo, So the Selector Snackbar Cannot Offer One
**Where:** `products/android/android-desktop/.../data/repository/ImageRepository.kt`, `ImageRepositoryImpl.kt`, `domain/usecase/MoveToSelectionUseCase.kt`, `viewmodel/SelectorViewModel.kt`, `ui/selector/SelectorActions.kt` (`SelectorSnackbar`)
**Debt:** The selector refresh specifies a 30-second undo window on Move / Copy / Delete, and `SelectorSnackbar` renders the countdown line for it, but `onUndo` is wired to `null` because nothing below the UI can reverse an action. `ImageRepository.moveImage`/`copyImage` return only `Boolean` and discard the destination URI, and `SelectorViewModel` stores `lastDeletedImage`/`lastDeletedIndex` but never uses them. An UNDO button that silently did nothing would be worse than none, so the affordance is currently absent rather than broken.
**Proposal:** Have `moveImage`/`copyImage` return the destination URI (or a small `FileOperationResult`) the way `MoveToSelectionUseCase.MoveResult` already does, and add `undoLastOperation()` to the view model that moves the file back / restores from MediaStore trash where `canTrash` is true. Then pass a non-null `onUndo` for the operations that are genuinely reversible and keep it null for the ones that are not — per `ai/skills/refactoring-guide/SKILL.md`, the result type belongs in the data layer, not in the composable.
**Status 2026-08-08:** The layer below the UI now exists. `ImageRepository.moveImage`/`copyImage` return `FileOperationResult` (`MoveResult` is an alias of it), `restoreFromTrash` and `undoMove` are implemented, and `domain/curation/` holds the pure logic (`DeferredDeletion`, `OptimisticEdits`, `UndoPolicy`) with JVM tests.
**Closed 2026-08-08 (UI pass):** `SelectorViewModel` now owns the snackbar message and an `UndoableOperation?`, both decided together so they cannot disagree. Move and Delete apply optimistically and transfer on `@ApplicationScope`; delete is deferred and committed when the window closes, another action arrives, the folder changes, or the ViewModel is cleared. `SelectorSnackbar.onUndo` is non-null exactly when `UndoPolicy` returns an operation — a copy never gets one. `lastDeletedImage`/`lastDeletedIndex` are deleted. Covered by `SelectorViewModelTest` (Robolectric, including the in-flight assertion gated on a `CompletableDeferred`) and two instrumented tests.

## [OPEN] 2026-07-27 - Scan Config, Score Legend and Settings Still Use the Pre-Refresh Visual Language
**Where:** `products/android/android-desktop/.../ui/selector/ScanConfigSheet.kt`, `ui/components/ScoreLegendSheet.kt`, `ui/settings/SettingsScreen.kt`, `ui/navigation/AdaptiveNavigation.kt`
**Debt:** The selector, filmstrip, chips and fullscreen viewer were rebuilt to the design handoff (right-side sheets, 72dp nav rail with 32dp app mark, two-pane settings, outlined-not-elevated surfaces), but these four surfaces were explicitly out of scope for that pass and still use Material defaults — `ModalBottomSheet` where the design specifies a 400/440dp right-side sheet, a single-pane settings list, and elevation where the rest of the app now uses 1dp outlines. The app currently reads as two different products depending on which surface is open.
**Proposal:** Second pass, one surface at a time, reusing the tokens and primitives already added in `ui/theme/Color.kt` and `ui/selector/SelectorChrome.kt` (`SelectorRail`, `RailButton`, `ImageTile`, `DetailsPanel`) rather than introducing parallel components. The score legend already carries the new `ScoreChip`, so it is the cheapest starting point.

## 2026-07-12 - GitHub Actions Workflow Rename Run Number Reset
**Learning:** Renaming or changing a GitHub Actions workflow filename resets the `github.run_number` count back to 1 for that new file. If `github.run_number` is used in version code calculations, the reset causes version code regressions. This will result in Google Play Store rollout errors (such as "does not allow any existing users to upgrade").
**Action:** When introducing or renaming a CI workflow that determines version codes, always add a baseline offset to ensure version codes remain strictly higher than any previously published builds.

## 2026-07-11 - Gradle Kotlin DSL 'java' Collision & GitHub Workflow Resilience
**Learning:** 
1. In Gradle Kotlin DSL, `java` is a reserved keyword referring to the Gradle Java plugin/extension. Referencing fully qualified names starting with `java.` (e.g. `java.util.Properties`) inside build files yields compiler errors like `Unresolved reference 'util'`.
2. When referencing repository configuration fields in GitHub Actions workflows, developers might store configuration values under either Secrets or Variables. Referencing them strictly via one scope can result in blank values if stored in the other.
**Action:** 
1. Place standard Java package imports at the very top of `.gradle.kts` files (e.g., `import java.util.Properties`) and instantiate them using their simple name (`Properties()`).
2. Implement workflow configuration fallbacks using the expression `${{ secrets.KEY || vars.KEY }}` to ensure the build remains resilient regardless of where the keys are stored.

## 2026-07-11 - Antigravity Workspace Customizations Root
**Learning:** Antigravity's customizations model discovers workspace rules and skills from the `ai/agents/` directory (when configured as the Workspace Customizations Root). It expects skills to be placed in `skills/<skill_name>/` relative to that root. Without a `.agents/skills` directory or symlink, workspace skills are ignored by Antigravity.
**Action:** Create and maintain a symlink `ai/agents/skills -> ../.skills` to ensure workspace skills are automatically discovered and loaded by the Antigravity IDE.

## 2026-07-23 - Group-Limited Navigation via Contiguous Candidate Blocks
**Learning:** In the desktop Sharpness tool, an *expanded* similarity group always occupies a contiguous block in `self.candidates` starting at its representative (see `apply_grouping_and_refresh`). This means "confine prev/next to the current group" needs no parallel data structure — it is just clamping the candidate index to `[start, start+len(files)-1]`. Deriving bounds from the existing candidate order (rather than iterating `group.files` in a possibly different sort order) keeps navigation consistent with the visible listbox and avoids an index-mapping bug where the representative is not first in `group.files`.
**Action:** When adding constrained navigation over an already-materialised ordered list, prefer clamping indices within a computed contiguous sub-range over introducing a second ordering. `_current_group_bounds()` centralises this so button-state, triplet loading, and fullscreen all share one definition.

## 2026-07-23 - Multi-Engine Aesthetic Scoring via a Dispatcher Tool
**Learning:** Replacing the single Ollama-backed `aesthetic` tool with several engines (Apple Vision, NIMA ONNX, Ollama) is cleanest as ONE registered dispatcher tool (`AestheticTool`) that reads an `aesthetic_engine` config key and delegates, rather than multiple classes all claiming the `aesthetic` registry key (last-registration-wins is fragile and import-order dependent). Engine availability probes and score-mapping are pure functions with injectable flags so they unit-test without native deps (PyObjC/onnxruntime). `auto` mode degrades to Ollama so existing installs keep working until a lighter engine is provisioned. On Android the mirror pattern is graceful degradation: `AestheticAnalyzer` returns null when no `.tflite` asset is bundled, so the feature is inert (no chip shown) until a model ships.
**Action:** For pluggable backends behind one logical capability, register a single dispatcher keyed by config and keep the decision logic pure/injectable. Guard native/model paths behind availability checks and prefer null/inert fallbacks over hard failures so the app builds and runs without the optional heavy assets.
**Extended 2026-08-09 (the Action above was half wrong):** `auto` was written to prefer Apple Vision, and it **never once selected it in an ordinary install** — `pyobjc-framework-Vision` was declared under `[project.optional-dependencies]`, so the probe failed everywhere, `auto` fell through to Ollama, and no surface said so. Measured on one machine: `llava:7b` **18.7 s/image against Apple Vision's 0.095 s/image, ~197×**, so weeks of silence reached the user as "the AI scoring is slow and bad" rather than as a missing dependency. **An optional extra is an engine that is off.** A dispatcher may only prefer a branch whose dependency is reachable in a *default* install: declare it as a real dependency with an environment marker (`… ; sys_platform == 'darwin'`) and reserve the extra for engines that are genuinely inert without user-supplied assets (`nima`, which needs a model file that no install can provide). Declaration is not reachability at the next boundary either — in a frozen artefact prove the import survives packaging, and measure the cheapest mechanism rather than buying the largest: two throwaway PyInstaller probe builds (~90 s) showed `--hidden-import` gives the same guarantee as `--collect-all` for **0 bytes instead of +2.2 MiB**, and that `--collect-all` drags `.dSYM` debug bundles into the shipped app. Finally, "prefer null/inert fallbacks over hard failures" only holds when the fallback can say **why**: every `auto` selection returns `(choice, reason)` from the outset and logs it (`tools/aesthetic.py::select_engine_with_reason`), and the surface offering the setting renders that reason. A graceful degradation that cannot state its cause is indistinguishable from a bad feature — which is precisely how this one was reported. The Android mirror named above (`AestheticAnalyzer` returning null when no `.tflite` is bundled) has the same silence and no reason string.

## 2026-07-23 - Multi-Module Selective CI Deployment via Path Filtering
**Learning:** In a repository containing multiple independent deployable applications sharing a CI pipeline (such as `.github/workflows/android.yml` for `:android-desktop` and `:phototok`), triggering release builds and Play Store / Firebase uploads unconditionally on `main` pushes re-packages and re-publishes unchanged applications. Using `dorny/paths-filter@v3` in an early `detect-changes` job allows downstream build and deployment steps to run selectively per module while preserving manual dispatch and release tag behavior.
**Action:** When a workflow manages multiple deployable modules, detect changes early with `dorny/paths-filter@v3` and guard downstream artifact creation and publishing steps with module outputs (`if: needs.detect-changes.outputs.<module> == 'true' || github.event_name == 'workflow_dispatch' || startsWith(github.ref, 'refs/tags/v')`).

## [OPEN] 2026-07-24 - Synchronous file I/O on the Tk main thread in move/copy to Selection
**Where:** `products/desktop/src/photo_selector_toolbox/gui/sharpness_tool.py` — `execute_move_to_selection`, `execute_copy_to_selection`
**Debt:** `execute_delete` already does its filesystem work in a background thread (per REQUIREMENTS §3.3 "Asynchronous Deletion"), updating the UI immediately so deletion feels instant. Move and Copy still call `f.rename(dest)` / `shutil.copy2` synchronously on the Tk main thread, for the image plus every related RAW/JPEG/XMP sibling. On a network share or a slow external drive this blocks the event loop for the whole transfer — the UI stops redrawing, keystrokes queue up, and on macOS it is a plausible contributor to the fullscreen viewer losing keyboard focus (see `ai/memory/palette.md` 2026-07-24). The two functions are also near-identical: ~90 lines duplicated between move and copy, differing only in `f.rename` vs `shutil.copy2`.
**Proposal:** Extract the shared destination-resolution and RAW/JPEG/XMP subfolder-sorting logic into one helper taking a transfer callable (see `ai/skills/refactoring-guide/SKILL.md` "centralized constants" and "controller/view separation"), then move the transfer loop onto a background thread mirroring `execute_delete`'s pattern: update `candidates`/listbox on the main thread first, do I/O off-thread, report failures back via `parent.after(0, ...)`. Needs care around the pending-delete/undo semantics and the fullscreen viewer's list pruning.

## [OPEN] 2026-07-24 - Desktop GUI test suite cannot run without a real tkinter
**Where:** `products/desktop/tests/unit/gui/test_sharpness_gui_basic.py`, `products/desktop/tests/unit/gui/test_fullscreen_features.py`, `products/desktop/tests/unit/gui/test_gui.py`, `products/desktop/tests/conftest.py`
**Debt:** The GUI tests replace `tk.Toplevel`/`ttk.*` with dummies but still `import tkinter` at module scope, so they cannot be collected at all on a Python build without the Tk bindings (common in slim CI images and sandboxes). Additionally `products/desktop/tests/unit/gui/test_sharpness_gui_basic.py` only passes when run as part of the full suite: its module-scoped fixture installs a `MagicMock` into `sys.modules["photo_selector_toolbox.controllers"]` before the real submodule is ever imported, so `patch("photo_selector_toolbox.controllers.X")` fails with `AttributeError: module 'photo_selector_toolbox' has no attribute 'controllers'` unless an earlier test file happened to import it first. Running that file alone yields 18 collection errors.
**Proposal:** Make the mock fixture set the attribute on the parent package as well as `sys.modules` (or import the real module first and patch attributes on it), so each test file is independently runnable. Consider a shared `products/desktop/tests/_tk_stub.py` installed from `conftest.py` when `tkinter` is unavailable, so the pure-logic GUI tests stay runnable in minimal environments.

## 2026-07-27 - androidTest Sources Are Only Compiled by the Emulator Job, So Typos Fail CI Late and Opaquely
**Learning:** `feat/score-chips-and-first-run-hints` failed `Instrumented tests (API 30)` on every run while `Unit tests (JVM)` and `Android Lint` stayed green. Nothing in `src/` or `tests/unit/` was wrong — `SelectorScreenTest.kt` had two unresolved references introduced alongside the score-legend feature: `hasTag("score_legend_button")` (the Compose matcher is `hasTestTag`; `hasTag` does not exist, and the file's `import androidx.compose.ui.test.*` wildcard hides that from review) and `ScanResult(filePath = item.path, …)` (`ImageItem` exposes `uri`, never `path`). Both break `:android-desktop:compileDebugAndroidTestKotlin`, so the job dies during the Gradle build inside the emulator step — the surfaced annotation is only "The process '/usr/bin/sh' failed with exit code 1", with the real Kotlin error buried in a log that needs repo auth to read. The job's short runtime (~3m30s, less than a normal boot-plus-test cycle) is the tell that the build, not a test, failed.
**Action:** `testDebugUnitTest` and `lintDebug` do **not** compile `tests/instrumented/`, so androidTest typos were invisible until the slowest, least-readable job ran. The JVM `unit-tests` job now carries a `Compile instrumented test sources` step (`./gradlew :android-desktop:assembleDebugAndroidTest :phototok:assembleDebugAndroidTest`) that validates the same sources — Kotlin plus kapt/Hilt processing — in ~2 minutes with no emulator, and reports plain compiler output. Keep that step in place, and keep it *after* `Run unit tests` so a test-source compile break never masks unit-test results. When an emulator job fails faster than it could plausibly have booted, read it as a build failure and check androidTest compilation first. Prefer explicit imports over `androidx.compose.ui.test.*` in test sources so a non-existent matcher is obvious at the import line.

## 2026-07-27 - An Unknown Input to a GitHub Action Is a Warning, Not an Error
**Learning:** Commit 879c9e6 added `disable-snapshot: true` to `reactivecircus/android-emulator-runner@v2` "to resolve emulator teardown crash on CI". That input does not exist on the action; it was silently ignored and only produced an `Unexpected input(s) 'disable-snapshot'` annotation, so the teardown fix was never actually applied and the `killall -9 crashpad_handler` workaround stayed load-bearing. Snapshot behaviour on this action is controlled through `emulator-options`, not a dedicated input.
**Action:** Treat "Unexpected input(s) …" annotations as build errors during review — a mistyped or invented input means the fix you believe you shipped is a no-op. Pass `-no-snapshot-save` via `emulator-options` for emulator teardown crashes, and check the action's declared inputs (the warning lists all valid ones) before adding a new key.

## [OPEN] 2026-07-31 - Swipe-Action Icon Mapping Duplicated Between the Viewer and the Controls Guide
**Where:** `products/android/phototok/src/com/phototok/ui/phonemode/PhoneModeViewer.kt` (`collectionIcon`, `leftSwipeIcon`) and `.../GestureTutorialOverlay.kt` (`collectionActionIcon`, `leftSwipeActionIcon`)
**Debt:** The `CollectionAction`/`SwipeAction` → `ImageVector` mapping now exists twice, once per file, with different function names and identical bodies. The *wording* for the same actions was deliberately centralised in the pure `com.phototok.domain.SwipeLabels`, but the glyphs could not follow it there because `ImageVector` is a Compose type and the domain layer must stay Android-free. Two copies means the guide and the mid-swipe indicator can start showing different icons for the same configured action — exactly the drift `SwipeLabels` was introduced to stop.
**Proposal:** Add a small `com.phototok.ui.components.SwipeActionIcons` object (or extend an existing shared components file) holding the two mappings and have both call sites use it, keeping the icon choice adjacent to `SwipeLabels` conceptually without polluting the domain layer. Mirrors `ai/skills/refactoring-guide/SKILL.md` "centralized constants". Cheap and mechanical; deferred only to keep the feature change reviewable.

## [OPEN] 2026-07-31 - PhoneModeViewModel Has Grown Past 900 Lines and Now Owns Folder Streaming Too
**Where:** `products/android/phototok/src/com/phototok/viewmodel/PhoneModeViewModel.kt`
**Debt:** The ViewModel now carries settings observation, folder selection, progressive discovery (batch de-duplication, append merge, deferred position restore, `publishedUris`/`dimensionsResolved` session sets), ordering, EXIF prefetch, batched dimension loading, optimistic copy/move with rollback, pending-delete lifecycle, first-run hints and tutorial state. The pure list logic is properly extracted into `com.phototok.domain`, so what is left is genuinely stateful coordination — but there are now four pieces of mutable session state (`publishedUris`, `dimensionsResolved`, `pendingRestoreIndex`, `userHasNavigated`) that must all be reset together in `selectSourceFolder` and `goBackToLanding`, and forgetting one is a silent bug (a stale `publishedUris` makes a new folder look empty).
**Proposal:** Extract a `FeedLoader` (or `FolderStreamSession`) collaborator owning the discovery lifecycle and that session state behind a single `reset()`, exposing batches as already-merged snapshots; the ViewModel keeps UI state and actions. Would also let the streaming behaviour be tested without Robolectric. Multi-file — schedule as a dedicated `@shared-code-health-agent` task rather than a retro side effect.

## [OPEN] 2026-08-08 - PhotoTok's Optimistic Copy/Move May Have the Same Snapshot-Merge Shape
**Where:** `products/android/phototok/src/com/phototok/viewmodel/PhoneModeViewModel.kt` (optimistic copy/move, progressive discovery merge)
**Debt:** Android Desktop's `restoreCachedScores` merged a *pre-IO snapshot* of the image list back over live state — `byUri[image.uri] ?: image` — which reverted EXIF and dimensions written by other coroutines during the IO window. Fixed there by `domain/session/ScoreMerge`, whose rule is: merge the field you computed, never the item you computed it from. PhotoTok does optimistic copy/move against a list it also re-reads from progressive discovery, so the ingredients are present; whether it merges items or fields has not been read. Recorded in `docs/shared/FEATURE_PARITY.md` as a `◐ gap` rather than assumed safe.
**Proposal:** Read the merge sites in `PhoneModeViewModel`. If any rebuilds a list from a snapshot captured before a suspend point, apply the same shape — a pure merge helper in `com.phototok.domain` taking only the computed field. Do **not** port `ScoreMerge` itself; PhotoTok has no scan results and the products do not share code. Cheap to check, and the failure mode is invisible in review.

## [OPEN] 2026-08-08 - Selector Layout Toggles Are Not Checked Against Hiding Their Own Control
**Where:** `products/android/android-desktop/src/com/photoselectortoolbox/ui/selector/SelectorControlBlock.kt`, `FrameGeometry.kt`
**Debt:** `details_toggle` hid the readout flank, which the solver then collapsed to 0dp — taking the control block, and therefore the toggle itself, off screen with no keyboard binding to recover. Fixed by flooring the flank at `MinimumControlBlockWidth`, and a unit test now asserts that floor for every toggle combination. But the *general* rule — a control that hides a region must not live in the region it hides — is enforced only for this one case. `filmstrip_toggle`, `overlay_values_toggle` and any future view toggle live in the same block and are one solver change away from the same trap.
**Proposal:** Either bind a key to `details_toggle` (cheap, and the guide already has a "keys with no button" callout that would carry it), or add an instrumented invariant that after toggling *every* view toggle off in sequence, `control_block` is still displayed. The second is the real guard; the first is worth doing anyway, since every other view-affecting control on this screen has a key.

## [OPEN] 2026-08-08 - GroupingLevel's User-Facing Wording Is Written Inline in Compose
**Where:** `products/android/android-desktop/src/com/photoselectortoolbox/ui/settings/SettingsScreen.kt:249-256`, `products/android/android-desktop/src/com/photoselectortoolbox/domain/grouping/GroupingLevel.kt`
**Debt:** The label and description for each `GroupingLevel` are produced by a `when (level)` inside the settings composable, so the enum does not own its own wording. This is the shape that `ScoreMetric` (glyph, label, description, direction) and the new `domain.guidance.SidebarAction` (label, description, meaning) both moved away from, and for the reason recorded in `palette.md` 2026-08-07 as extended: a second mapping of the same concept is invisible to a totality test and drifts from the first. Found by the grep that entry's Action now prescribes — it was the only remaining instance in this product; `ScoreMetricIcon` has exactly one mapping again, and `FilingAction`/`SelectorHint` are already domain-owned.
**Proposal:** Move `label` and `description` onto `GroupingLevel` as constructor properties and have `SettingsScreen` read them, matching `SidebarAction`. Cheap and mechanical. Worth doing before anything else needs to name a grouping level — the guide is the obvious next caller, and that is exactly how the score glyphs ended up with two vocabularies.
